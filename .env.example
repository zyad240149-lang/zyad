# Copy to .env and fill in real values. Never commit .env itself.

# --- Server-only (never prefix with VITE_) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Client-safe (bundled into the browser — anon/public key only) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
